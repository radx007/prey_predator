package com.reactive.preypredator.model;

/**
 * Enum for controlling agent placement strategy
 */
public enum PlacementMode {
    RANDOM,          // Random placement each run
    FIXED_PATTERN    // Deterministic pattern for reproducible testing
}
